const Notification = (props) => {
  const style = {
    border: 'solid',
    padding: 10,
    borderWidth: 1,
    display: 'none'
  }
  return (
    <div style={style}>
      render here notification...
    </div>
  )
}

export default Notification